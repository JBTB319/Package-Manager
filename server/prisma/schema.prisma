generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Package {
  id        Int      @id @default(autoincrement())
  name      String
  version   String
  createdAt DateTime @default(now())
}
